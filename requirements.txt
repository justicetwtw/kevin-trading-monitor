# Core
pandas>=2.0.0
numpy>=1.24.0                 # pandas-ta-classic 0.5.44+ 已相容 numpy 2.x;Phase 4 indicators 會做 RSI/BB 實測再決定是否回鎖

# Data sources
yfinance>=0.2.40
pandas-datareader>=0.10.0
fredapi>=0.5.1
edgartools>=2.0.0
feedparser>=6.0.0
httpx>=0.25.0
selectolax>=0.3.17
beautifulsoup4>=4.12.0           # 部分 scraping 用 BS4 較穩
requests>=2.31.0

# Technical analysis (pandas-ta 已停止維護,改用社群維護的 fork pandas-ta-classic)
pandas-ta-classic>=0.5.0
scipy>=1.11.0

# Storage / private state
pyarrow>=14.0.0
cryptography>=43.0.0             # Fernet encryption for private drawdown high-water state

# Telegram
python-telegram-bot>=20.0

# Gooaye Podcast Digest (gooaye-digest) — Gemini 音檔轉錄 + 摘要
google-genai>=1.0.0              # 新版統一 SDK(from google import genai),非舊版 google-generativeai

# Twstock(台股)
twstock>=1.3.0                   # 台股報價套件(yfinance 替補)

# Utilities
pytz>=2024.1
python-dateutil>=2.8.2
tenacity>=8.2.0
loguru>=0.7.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Backtesting (Phase 3 — 暫不安裝,Phase 3 開工再啟用)
# vectorbt>=0.26.0
